describe("general helpers", function () {
    describe("addLogFunction", function () {
        it("adds a log function to an object", function () {});
        it("does not let newly added functions be overwritten", function () {});
    });
});
